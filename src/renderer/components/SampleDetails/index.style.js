import styled from 'styled-components';

export const WrapperStl = styled.div`
  width: 30vw;
  border-left: 1px solid rgba(0, 0, 0, 0.25);
  padding: 2em;
`;

export const HeaderStl = styled.div`
  display: flex;
  flex-direction: row;
`;

export const DownloadStl = styled.div`
  flex: 0;
  margin-left: auto;
`;
