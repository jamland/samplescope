const BASE_URL = 'https://freesound.org';
const API_VERSION = 'apiv2';

const API = {
  SEARCH_TEXT: `${BASE_URL}/${API_VERSION}/search/text/`,
  SOUNDS: `${BASE_URL}/${API_VERSION}/sounds/`,
};

export default API;
